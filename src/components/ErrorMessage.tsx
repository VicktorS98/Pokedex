import React from "react"

export default function ErrorMessage() {
  return (
    <p style={{textAlign: "center", marginTop: "3rem", fontSize: "var(--fs-8"}}>How weird... we couldn´t find that Pókemon in our database</p>
  )
};